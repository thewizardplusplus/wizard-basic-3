note Arg(array, index, default = NULL)
function Arg(args)
	let array = args[0]
	let index = args[1]

	let default = NULL
	if GetSize(args) > 2 then
		default = args[2]
	end

	if index < GetSize(array) then
		return array[index]
	else
		return default
	end
end
