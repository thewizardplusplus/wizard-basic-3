include "range/utils"
include "io"

function Append(array_1, array_2)
	let result = new array(GetLength(array_1) + GetLength(array_2))
	let i = Range([result])
	while Next(i) do
		if i.index < GetLength(array_1) then
			result[i.index] = array_1[i.index]
		else
			result[i.index] = array_2[i.index - GetLength(array_1)]
		end
	end

	return result
end

function Slice(array, start, finish)
	if start < 0 or finish > GetLength(array) then
		Die("Error: out of range.")
	end

	let result = new array(finish - start)
	let i = Range([result])
	while Next(i) do
		result[i.index] = array[i.index + start]
	end

	return result
end
